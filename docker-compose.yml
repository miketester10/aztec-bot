services:
  AztecBot:
    image: node:24.2.0-alpine
    container_name: AztecBot
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    command: ["sh", "-c", "npm install && npm run start"]